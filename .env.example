# Cloudflare Worker URLs
# These are the default worker URLs, you can override them if needed

# Holidays Worker URL - fetches holiday data from multiple APIs
VITE_HOLIDAYS_WORKER_URL=https://holidays.orangely.workers.dev

# Holiday Info Worker URL - fetches detailed holiday information using AI
VITE_HOLIDAY_INFO_WORKER_URL=https://holiday-info.orangely.workers.dev

# Instructions:
# 1. Copy this file to .env
# 2. Replace the URLs with your actual Cloudflare Worker URLs if needed
# 3. Note: Vite uses VITE_ prefix for environment variables
# 4. The default URLs point to working Cloudflare Workers